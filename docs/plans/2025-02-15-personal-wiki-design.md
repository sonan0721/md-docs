# Personal Wiki 설계 문서

> **Local-first, AI-friendly, MD-based Personal Wiki**

## 개요

개인 지식 저장소로서의 위키 시스템. 로컬 MD 파일을 기반으로 하여 AI 도구가 쉽게 접근/편집할 수 있으며, 웹 인터페이스로 어디서든 접근 가능.

### 핵심 원칙

1. **Local MD = Source of Truth** — 모든 데이터는 MD 파일, DB 없음
2. **오프라인 우선** — 로컬에서 완전 독립 작동
3. **AI 친화적** — 표준 MD, 평문 파일, 스크립트 접근 가능
4. **점진적 복잡도** — 단순하게 시작, 필요시 확장
5. **확장 가능 구조** — 다른 사용자도 자신의 repo 연결 가능

---

## 요구사항 요약

| 항목 | 결정 |
|------|------|
| 철학 | 로컬 우선, MD 기반 |
| 동기화 | Git (GitHub) |
| 호스팅 | Vercel (정적 + Edge Functions) |
| 인증 | GitHub OAuth |
| 에디터 | TipTap 기반 중간형 WYSIWYG |
| 확장 문법 | BBCode (스타일+레이아웃) + `[[백링크]]` |
| 구조 | 폴더 기본 + 태그 혼용 |
| 검색 | FlexSearch + 한글 형태소 + 퍼지 |
| 부가 기능 | ToC, 백링크, 하위 문서 표시 |
| 모바일 | 읽기 위주 반응형 |
| 공개 | 선택적 public/private |
| 기술 스택 | Next.js + React + TipTap |

---

## 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                        사용자                                │
├─────────────┬─────────────────────────┬─────────────────────┤
│   로컬 편집  │       웹 편집            │      AI 편집         │
│  (VS Code,  │    (WYSIWYG)           │   (Claude Code,     │
│   Obsidian) │                        │    스크립트)          │
└──────┬──────┴───────────┬─────────────┴──────────┬──────────┘
       │                  │                        │
       ▼                  ▼                        ▼
┌─────────────────────────────────────────────────────────────┐
│                 로컬 MD 파일 시스템                           │
│   ~/wiki/                                                   │
│   ├── wiki.config.json                                      │
│   ├── content/                                              │
│   ├── assets/                                               │
│   └── .wiki/                                                │
└──────────────────────────┬──────────────────────────────────┘
                           │ git push/pull
                           ▼
┌─────────────────────────────────────────────────────────────┐
│              GitHub Private Repo                            │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                    Vercel                                   │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │ Static CDN  │  │Edge Function│  │  ISR/SSG    │         │
│  │ (페이지)    │  │ (OAuth)     │  │  (빌드)     │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
└─────────────────────────────────────────────────────────────┘
```

---

## 로컬 파일 구조

```
~/my-wiki/
├── wiki.config.json          # 위키 설정
├── content/                   # 모든 문서
│   ├── index.md              # 홈페이지
│   ├── inbox/                # 빠른 메모용
│   ├── projects/
│   │   ├── _folder.json      # 폴더 메타데이터
│   │   └── *.md
│   └── knowledge/
├── assets/                    # 이미지, 첨부파일
│   └── images/
└── .wiki/                     # 자동 생성 (gitignore)
    ├── search-index.json
    └── backlinks.json
```

### wiki.config.json

```json
{
  "name": "My Wiki",
  "repo": "username/my-wiki",
  "defaultFolder": "content",
  "theme": "light",
  "visibility": "private",
  "publicPaths": ["knowledge/public/**"],
  "features": {
    "bbcode": true,
    "backlinks": true,
    "tags": true
  }
}
```

### 문서 포맷 (Frontmatter + MD)

```markdown
---
title: 프로젝트 A 설계
tags: [project, design, 2024]
created: 2024-02-15
updated: 2024-02-15
public: false
---

# 프로젝트 A 설계

본문 내용...
[[관련 문서]] 링크
[color=blue]BBCode 스타일[/color]
```

---

## UI/UX 디자인

### 디자인 철학

- Notion의 깔끔함 + Outline의 미니멀
- 중앙 정렬 콘텐츠 (max-width 720px)
- 넉넉한 여백과 line-height (1.7)

### 3-Column 레이아웃

```
┌──────────────────────────────────────────────────────────────────┐
│  Logo   My Wiki                          🔍 Search ⌘K    👤 User │
├──────────────────────────────────────────────────────────────────┤
│           │                                  │                   │
│  SIDEBAR  │         CONTENT AREA             │   RIGHT PANEL     │
│   240px   │          max 720px               │      200px        │
│           │                                  │                   │
│  📥 Inbox │  # Document Title                │  ON THIS PAGE     │
│           │                                  │  • Section 1      │
│  📁 Folders│  Content...                     │  ● Section 2 ←    │
│    ├─ A   │                                  │  • Section 3      │
│    └─ B   │                                  │                   │
│           │                                  │  🔗 BACKLINKS     │
│  🏷️ Tags  │  📎 하위 문서                     │  • Doc A          │
│           │  [Sub1] [Sub2] [Sub3]            │  • Doc B          │
│           │                                  │                   │
└───────────┴──────────────────────────────────┴───────────────────┘
```

### 반응형

| 화면 크기 | 왼쪽 사이드바 | 콘텐츠 | 오른쪽 패널 |
|-----------|---------------|--------|-------------|
| Desktop (>1280px) | 240px 고정 | 720px 중앙 | 200px 고정 |
| Laptop (1024-1280px) | 240px 고정 | flex | 숨김, 콘텐츠 상단 ToC |
| Tablet (768-1024px) | 오버레이 | 전체 | 숨김 |
| Mobile (<768px) | 오버레이 | 전체 | 숨김, 접이식 ToC |

### 컬러 시스템

```
Light Mode              Dark Mode
Background: #FFFFFF     Background: #191919
Surface:    #F7F7F5     Surface:    #252525
Border:     #E8E8E8     Border:     #333333
Text:       #1A1A1A     Text:       #EBEBEB
Muted:      #6B6B6B     Muted:      #888888
Accent:     #2563EB     Accent:     #3B82F6
```

### 타이포그래피

```
Font Stack:
  - 본문: "Pretendard", "Apple SD Gothic Neo", sans-serif
  - 코드: "JetBrains Mono", "Fira Code", monospace

Sizes:
  - H1: 2rem (32px), font-weight: 700
  - H2: 1.5rem (24px), font-weight: 600
  - H3: 1.25rem (20px), font-weight: 600
  - Body: 1rem (16px), line-height: 1.7
```

---

## 에디터

### 기술 선택: TipTap

- Notion 스타일 블록 에디터
- MD 입출력
- 확장성 (BBCode, 백링크)

### 기능

**Floating Toolbar (선택 시)**
- Bold, Italic, Underline, Strikethrough
- Link, Code, Comment

**슬래시 명령어**
- `/h1`, `/h2`, `/h3` — 제목
- `/bullet`, `/number`, `/checklist` — 목록
- `/code`, `/quote` — 블록
- `/table`, `/image`, `/divider` — 삽입
- `/callout`, `/spoiler`, `/columns`, `/tabs` — BBCode 레이아웃

**백링크**
- `[[` 입력 시 자동완성
- 존재하지 않는 문서는 생성 옵션 제공
- 빨간 점선으로 broken link 표시

### BBCode 지원

**스타일링**
```bbcode
[color=red]빨간 텍스트[/color]
[size=20]큰 텍스트[/size]
[highlight=yellow]하이라이트[/highlight]
```

**레이아웃**
```bbcode
[box title="알림" type="info"]내용[/box]
[spoiler title="자세히"]숨김 내용[/spoiler]
[columns][col]열1[/col][col]열2[/col][/columns]
[tabs][tab title="탭1"]내용1[/tab][/tabs]
```

---

## 검색

### 기술 스택

- **인덱싱**: 빌드 타임에 FlexSearch 인덱스 생성
- **한글 처리**: es-hangul 또는 korean-tokenizer
- **저장**: search-index.json → IndexedDB 캐시

### 기능

1. **풀텍스트 검색** — 제목, 본문, 태그
2. **초성 검색** — ㅍㄹㅈ → 프로젝트
3. **퍼지 매칭** — 오타 허용
4. **고급 문법** — `tag:design`, `in:projects`, `"정확한 구문"`

### 가중치

| 검색 대상 | 가중치 |
|-----------|--------|
| 제목 | 3.0 |
| 태그 | 2.5 |
| 헤딩 (H1-H3) | 2.0 |
| 본문 | 1.0 |
| 파일 경로 | 0.5 |

### UI

- `⌘K` 단축키로 열기
- 실시간 검색 결과
- 최근 검색 히스토리
- 태그/폴더 필터 칩

---

## 인증 & 동기화

### GitHub OAuth 흐름

1. 사용자 → "GitHub으로 로그인" 클릭
2. GitHub 인증 페이지로 리다이렉트
3. 인증 완료 → Vercel Edge Function에서 토큰 교환
4. httpOnly cookie에 토큰 저장
5. 클라이언트에서 GitHub API 직접 호출

### 토큰 관리

- Access Token: httpOnly secure cookie
- 사용자 메타데이터: localStorage
- 401 응답 시 자동 로그아웃

### 동기화

**웹 → GitHub**
```
편집 완료 → GitHub API PUT contents → Commit 생성 → Webhook → Vercel 재빌드
```

**로컬 → GitHub → 웹**
```
로컬 편집 → git push → GitHub Actions → Vercel Deploy Hook → 재빌드
```

### 충돌 처리

- 저장 전 SHA 확인
- 불일치 시 충돌 UI 표시
- 옵션: 내 것 사용 / 원격 것 사용 / 수동 병합

### 오프라인 지원

- Service Worker + IndexedDB
- 최근 문서 캐시
- 오프라인 편집 → 온라인 복귀 시 자동 푸시

---

## 공개/비공개

### 가시성 모델

- **기본값**: Private
- **공개 설정 방법**:
  1. 문서 frontmatter: `public: true`
  2. 폴더 _folder.json: `public: true`
  3. 전역 wiki.config.json: `publicPaths: ["blog/**"]`

### 우선순위

```
문서 frontmatter > 폴더 _folder.json > 전역 publicPaths > 기본값(private)
```

### URL 구조

- Private: `https://wiki.example.com/docs/projects/project-a` (인증 필요)
- Public: `https://wiki.example.com/p/project-a` (짧은 URL)
- Public SEO: `https://wiki.example.com/public/projects/project-a`

### Public 페이지 제한

| 기능 | Private | Public |
|------|---------|--------|
| 읽기 | ✓ | ✓ |
| 편집 | ✓ | ✗ |
| 백링크 | 전체 | Public만 |
| 검색 | 전체 | Public만 |

---

## 기술 스택

### Frontend

- **Framework**: Next.js 14+ (App Router)
- **UI**: React 18+
- **Styling**: Tailwind CSS
- **Editor**: TipTap
- **Search**: FlexSearch + es-hangul
- **State**: Zustand 또는 Jotai

### Deployment

- **Hosting**: Vercel
- **Auth**: Vercel Edge Functions (GitHub OAuth)
- **Storage**: GitHub Repository
- **CDN**: Vercel Edge Network

### Build

- **SSG**: Next.js Static Export / ISR
- **Search Index**: 빌드 타임 생성
- **Backlinks**: 빌드 타임 분석

---

## 알려진 제약사항

| 제약 | 심각도 | 완화책 |
|------|--------|--------|
| Git 충돌 | 중 | 혼자 쓰면 드묾, 충돌 UI 제공 |
| 빌드 딜레이 | 하 | 클라이언트 프리뷰로 커버 |
| GitHub API 한계 | 하 | 개인 사용량으론 충분 |
| 대용량 repo | 중 | Git LFS, lazy loading |
| 백링크 깨짐 | 중 | 이름 변경 시 자동 업데이트 스크립트 |

---

## 다음 단계

1. 프로젝트 초기 설정 (Next.js + TypeScript + Tailwind)
2. 기본 레이아웃 구현 (3-column)
3. MD 파싱 & 렌더링
4. GitHub OAuth 연동
5. 에디터 구현 (TipTap)
6. 검색 구현
7. 배포 & 테스트
